# Pull request

## Summary

<!-- What changed and why -->

## Breaking change?

- [ ] No
- [ ] Yes (bump `x` in `0.x.y`; after `1.0`, bump major)

## Tested?

Required:

- [ ] `julia --project=. -e 'using Pkg; Pkg.test()'` passed

If applicable:

- [ ] Manual smoke (e.g. `drive local:2 demos/with_kit/square_file.jl`)
- [ ] Remote: CI **SSH E2E**, or local per `testenv/docker-ssh/README.md`

## Notes

<!-- Reviewer notes, limitations (optional) -->
