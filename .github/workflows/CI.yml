name: Test

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

# Cancel redundant runs on the same branch/PR.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

permissions:
  contents: read
  id-token: write # Codecov OIDC upload (see Upload coverage step)

jobs:
  # Cheap load + `--help` on macOS/Windows after merge (and on dispatch).
  # Full `Pkg.test()` is Linux and WSL2; PRs do not wait on macOS/Windows boot.
  boot-macos:
    if: github.event_name != 'pull_request'
    runs-on: macos-latest
    timeout-minutes: 15
    name: boot macOS
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: "1.12"
      - uses: julia-actions/cache@v3
      - uses: julia-actions/julia-buildpkg@v1
      - name: Load package
        run: julia --project=. -e "using DistSSHKit"
      - name: CLI help
        run: |
          julia --project=. -m DistSSHKit -h
          julia --project=. -m DistSSHKit drive -h
          julia --project=. -m DistSSHKit go -h
          julia --project=. -m DistSSHKit setup -h
          julia --project=. -m DistSSHKit size -h

  boot-windows:
    if: github.event_name != 'pull_request'
    runs-on: windows-latest
    timeout-minutes: 15
    name: boot Windows
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: "1.12"
      - uses: julia-actions/cache@v3
      - uses: julia-actions/julia-buildpkg@v1
      - name: Load package
        run: julia --project=. -e "using DistSSHKit"
      - name: CLI help
        run: |
          julia --project=. -m DistSSHKit -h
          julia --project=. -m DistSSHKit drive -h
          julia --project=. -m DistSSHKit go -h
          julia --project=. -m DistSSHKit setup -h
          julia --project=. -m DistSSHKit size -h

  # WSL2 Ubuntu on windows-latest. Copy onto the Linux filesystem (not /mnt/c).
  # Pkg.test as a Linux controller on WSL; not native Windows, not SSH E2E.
  wsl:
    runs-on: windows-latest
    timeout-minutes: 45
    name: WSL2 (Windows)
    steps:
      - uses: actions/checkout@v7
      - uses: Vampire/setup-wsl@v7
        with:
          distribution: Ubuntu-24.04
          wsl-version: 2
          additional-packages: ca-certificates curl git
      - name: Pkg.test in WSL2
        shell: wsl-bash {0}
        run: |
          src="$(pwd)"
          dest="$HOME/DistSSHKit.jl"
          rm -rf "$dest"
          git clone "$src" "$dest"
          curl --retry 5 --retry-delay 5 --retry-connrefused --connect-timeout 10 \
            -fsSL https://install.julialang.org | sh -s -- --yes --default-channel 1.12
          export PATH="$HOME/.juliaup/bin:$PATH"
          cd "$dest"
          julia --project=. -e 'using Pkg; Pkg.test()'

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    name: ${{ matrix.label }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - version: "1.12"
            label: "Julia 1.12"
          # 1.13 is still a prerelease channel; ~1.13.0-0 picks the latest 1.13.x (including RCs).
          - version: "~1.13.0-0"
            label: "Julia 1.13"
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: ${{ matrix.version }}
      - uses: julia-actions/cache@v3
      - uses: julia-actions/julia-buildpkg@v1
      - uses: julia-actions/julia-runtest@v1
        with:
          coverage: ${{ matrix.version == '1.12' }}
      - uses: julia-actions/julia-processcoverage@v1
        if: matrix.version == '1.12'
      # Public: OIDC. Private: upload fails quietly until the repo is public.
      - name: Upload coverage to Codecov
        if: matrix.version == '1.12'
        uses: codecov/codecov-action@v7
        with:
          files: lcov.info
          slug: ${{ github.repository }}
          use_oidc: true
          fail_ci_if_error: false
      - name: Coverage summary
        if: matrix.version == '1.12'
        run: |
          julia --color=no -e '
          using Pkg
          Pkg.add("Coverage")
          using Coverage, Printf
          files = process_folder("src")
          for fc in files
              c, t = get_summary(fc)
              t == 0 && continue
              @printf("%-32s %5.1f%%\n", basename(fc.filename), 100 * c / t)
          end
          c, t = get_summary(files)
          @printf("%-32s %5.1f%%  (%d/%d lines)\n", "TOTAL", 100 * c / t, c, t)
          '
